# 多 Agent 协同运营自动化系统 MVP 项目说明

本项目是一个基于 Agent 和 AI 驱动思想构建的多 Agent 协同运营自动化系统 MVP，目标是模拟真实运营团队在项目冷启动、活动推广、用户增长和内容投放过程中的协作方式。系统并不是简单地生成一段文案，而是将运营工作拆解为多个具有明确职责的智能角色，由编排器统一调度，使用户只需要输入项目名称、产品服务、目标用户、预算、运营目标和内容语气，系统就可以自动生成一套较完整的运营执行方案，包括目标拆解、用户画像、渠道策略、推广文案、风险审核、七天排期、指标预测和复盘建议。

## 一、项目背景与核心痛点

在实际运营工作中，一个完整的活动方案往往需要多人协作完成。策划人员需要拆解目标，市场人员需要分析用户，内容人员需要撰写文案，运营人员需要安排发布时间和执行节奏，负责人还需要检查风险、评估效果并组织复盘。对于小团队、个人开发者、校园项目或早期创业项目来说，这种流程成本较高，沟通链路较长，而且方案质量容易受到人员经验影响。

传统方式下，运营方案经常存在三个问题。第一，流程不够标准化，不同人写出的方案结构差异很大，后续执行难以统一。第二，信息传递容易断层，例如用户画像没有很好地传递到文案环节，渠道策略也没有落实到具体排期。第三，复盘缺少数据意识，很多活动执行完之后只停留在主观总结，无法形成可复用的 SOP。本项目希望通过多 Agent 协作，把这些分散的运营环节整合成一条自动化工作流，提高方案生成效率，也让运营执行更有条理。

## 二、核心功能说明

系统内置七个核心 Agent。PlannerAgent 负责运营规划，主要完成目标拆解、阶段划分和任务优先级设计；ResearchAgent 负责用户洞察，分析目标用户的痛点、动机、使用场景和核心价值主张；ChannelAgent 负责渠道策略，根据目标用户和预算选择合适的推广渠道，并为每个渠道设计动作方向；CopywriterAgent 负责内容生产，生成适配小红书、公众号、社群、邮件等场景的运营文案；ReviewAgent 负责风险审核，检查文案中是否存在夸大宣传、表达不清、转化链路缺失等问题；SchedulerAgent 负责执行排期，将策略和文案整理成七天运营日历；AnalystAgent 负责数据预测与复盘建议，输出曝光、点击、转化等指标预估，并提出下一步优化方向。

这些 Agent 按照真实业务顺序依次执行，前一个 Agent 的结果会成为后一个 Agent 的上下文输入。例如，用户洞察结果会影响渠道选择，渠道策略会影响文案内容，审核意见会影响最终排期，数据预测又会反过来帮助团队判断方案是否可执行。这种设计体现了长链推理的思路，也让 AI 生成结果更加接近真实工作流程。

## 三、技术架构与实现方式

项目后端使用 Python 和 FastAPI 构建，负责接口服务、Agent 调度、数据处理和结果返回。SQLite 用于保存项目数据、Agent 执行日志和生成结果，方便用户查看历史记录。前端使用 HTML、CSS 和 Vanilla JavaScript 实现，界面简洁，用户可以在页面中填写项目信息、启动工作流、查看 Agent 执行过程，并导出完整运营方案。模板渲染使用 Jinja2，整体结构清晰，适合继续扩展。

系统核心由 Orchestrator 编排器控制。编排器会按照固定顺序调用各个 Agent，并通过 BlackboardMemory 黑板记忆机制共享上下文。每个 Agent 执行完成后，都会把结果写入共享记忆，同时记录到数据库日志中。最终系统会把所有阶段的结果整合为一个结构化方案，包含项目总览、运营规划、用户洞察、渠道策略、推广文案、审核意见、执行排期、数据分析和 Agent 协作轨迹。

项目还支持两种 LLM 模式。默认情况下使用本地规则模型 Mock LLM，不需要任何 API Key 就可以直接运行，适合演示、答辩和本地测试。如果用户拥有 OpenAI API Key，也可以通过 .env 配置切换为真实大模型模式，让系统输出更加灵活和自然。

## 四、运行方式

解压项目后，进入项目根目录，先创建 Python 虚拟环境并安装依赖：

```bash
python -m venv .venv
pip install -r requirements.txt
```

Windows 用户可以使用：

```bash
.venv\Scripts\activate
```

macOS 或 Linux 用户可以使用：

```bash
source .venv/bin/activate
```

安装完成后执行：

```bash
python run.py
```

启动成功后，在浏览器访问：

```text
http://127.0.0.1:8000
```

如果需要接入真实大模型，可以复制 `.env.example` 为 `.env`，填写 `LLM_PROVIDER`、`OPENAI_API_KEY` 和模型名称后重新启动服务。

## 五、项目亮点与应用价值

本项目的亮点在于把 AI 从单次问答工具扩展为流程型业务助手。它不仅能生成内容，还能按照运营业务逻辑完成多步骤推理和多角色协作。项目具有清晰的 Agent 分工、可追踪的执行日志、可持久化的数据结果和完整的前后端交互界面。对于课程设计、创新创业项目、AI 应用展示、运营自动化演示和多 Agent 架构学习都有较高参考价值。

未来可以继续扩展真实数据接入、自动读取平台投放数据、支持更多模型供应商、增加可视化看板、加入人工审核节点，并进一步实现从方案生成到执行跟踪、数据回流、自动优化的完整闭环。整体来看，该项目展示了 AI 在运营自动化、业务流程编排和智能协作系统中的实际落地能力。